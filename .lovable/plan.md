

## Add Google Site Verification Meta Tag

**File: `index.html`**

Add the following meta tag inside the existing `<head>` section:

```html
<meta name="google-site-verification" content="Tz9xDtQyDY22LKK75w9KCPtobC-QuMpdBwBXFcUvnRw" />
```

Place it right after the existing `<meta name="author">` tag (line 16). Single-line change, no other files affected.

